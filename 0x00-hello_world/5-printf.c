#include <stdio.h>

/**
 * main - Entry Piont
 *
 * description: a C program that prints a quote with printf function.
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
